# shortcuts
alias ls='ls -F'

# show git branch in shell prompt (from: http://betterexplained.com/articles/aha-moments-when-learning-git/ and
# http://asemanfar.com/Current-Git-Branch-in-Bash-Prompt)
parse_git_branch()
{
        git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'
}
export PS1="\[\033[00m\]\u@\h\[\033[01;34m\] \W \[\033[31m\]\$(parse_git_branch) \[\033[00m\]$\[\033[00m\] "

####
# to get colors in terminal (see: http://it.toolbox.com/blogs/lim/how-to-fix-colors-on-mac-osx-terminal-37214):
# enables coloring of your terminal:
export CLICOLOR=1
# specifies how to color specific items:
export LSCOLORS=GxFxCxDxBxegedabagaced

# source specific profile if provided
if [ -e "$HOME/.specific_profile" ]
then
  source "$HOME/.specific_profile"
fi

# postgres
export PGDATA="/usr/local/var/postgres"
alias pgstart="pg_ctl start -D $PGDATA --log=$HOME/.pgserver_log"
alias pgquit="pg_ctl stop  -D $PGDATA -s -m fast"
pg_ctl status &> /dev/null
if [ $? -ne 0 ]
then
  pgstart
fi

# source git completion script https://github.com/git/git/tree/master/contrib/completion
source ~/.git-completion.bash

# python env
export CONDA_DIR=$HOME/.conda
export PATH=/usr/local/Cellar:/usr/local/bin:/usr/local/sbin:$PATH
if [ -d $CONDA_DIR ]
then
  export PATH=$CONDA_DIR/bin:$PATH
fi

# ruby
eval "$(rbenv init -)"

